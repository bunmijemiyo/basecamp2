
<h1 class="text-center" >Projects Listing page</h1>

<div class="flickr_pagination">
    <%= will_paginate @projects, :container => false %>
  </div>

<%= render 'project' %>

<div class="flickr_pagination mb-4">
    <%= will_paginate @articles, :container => false %>
  </div>